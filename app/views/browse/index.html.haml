%h1 MyDictionary

= form_tag browse_index_path, :method => "get" do 
  = text_field_tag "url"
  = submit_tag

%iframe{:height => 1000, :width => "70%", :src => @iframe_url}

%p
  = form_tag browse_index_path, :method => "get" do
    = text_field_tag "word"
    = submit_tag

  = raw(@search_result)
